<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title></title>
</head>
<body>
  <p>
    Hello, there is a new product published
  </p>
  <div>
    <h3>Images</h3>
    <% @product.images.each do |image| %>
      <%= image_tag(url_for(image.avatar), size: '300x300') %>
    <% end %>
  </div>
  <p>
    <%= link_to 'See product', product_url(@product) %>
  </p>
  <p>
    <%= link_to 'See all products', products_url %>
  </p>
</body>
</html>
